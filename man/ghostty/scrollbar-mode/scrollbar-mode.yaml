system: |
  Respect the system settings for when to show scrollbars. Follows the desktop environment's
  scrollbar visibility preference (e.g. always show, show only when scrolling, etc.).
never: |
  Never show a scrollbar. The scrollbar is completely hidden, though scrolling via keyboard,
  mouse wheel, or touchpad still works.
