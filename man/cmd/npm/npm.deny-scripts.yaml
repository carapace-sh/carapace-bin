# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: deny-scripts
description: Deny install scripts for specific dependencies
flags:
    --allow-scripts-pin: write pinned entries when denying install scripts
    --json: output as json
    -a, --all: act on all packages with pending install scripts
