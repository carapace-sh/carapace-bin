# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: Lists all available packages
flags:
    -o, --output=: 'Defines the output format (valid: ''list'', ''json'')'
    -v, --verbose: Show more information
