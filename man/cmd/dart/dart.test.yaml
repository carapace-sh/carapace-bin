# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: test
description: Run tests for a project

documentation:
  command: >-
    Run unit tests for the current project. Supports --coverage, --chain-stack-traces, and test name filtering.
