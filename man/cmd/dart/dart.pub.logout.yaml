# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: logout
description: Log out of pub.dev
flags:
    -h, --help: Print this usage information.

documentation:
  command: >-
    Delete stored OAuth2 credentials for pub.dev. After logging out, you must re-authenticate before publishing packages.
