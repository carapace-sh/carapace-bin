# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: version
description: Show Scalar version
flags:
    --build-options: also print build options
    -v, --verbose: include Git version

documentation:
  command: >-
    Show the Scalar version.
