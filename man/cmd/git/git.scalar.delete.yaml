# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: delete
description: Delete an existing Scalar enlistment

documentation:
  command: >-
    Delete an existing Scalar enlistment from the local file system, unregistering the repository.
