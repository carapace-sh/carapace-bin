# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: refs
description: Low-level access to refs
group: manipulator
documentation:
  command: This command provides low-level access to refs.
