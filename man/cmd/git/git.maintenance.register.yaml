# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: register
description: Initialize Git config values
flags:
  --config-file=: use given config file
documentation:
  flag:
    config-file: use given config file
