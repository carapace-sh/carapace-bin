# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: info
description: displays information about a CHD
flags:
    -i, --input=: input file name
    -v, --verbose: output additional information
