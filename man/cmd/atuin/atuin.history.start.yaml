# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: start
description: Begins a new command in the history
flags:
    --author=: Author of this command, eg `ellie`, `claude`, or `copilot`
    --intent=: Optional intent/rationale for running this command
    -h, --help: Print help
