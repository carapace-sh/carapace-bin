# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: config
description: Manage the configuration of the New Relic CLI

documentation:
  command: >-
    Manage the New Relic CLI configuration, including default region, output format, and display settings.
