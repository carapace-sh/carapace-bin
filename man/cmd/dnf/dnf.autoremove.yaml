# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: autoremove
description: remove unneeded packages from the system
flags:
    --offline: store the transaction to be performed offline
    --store=: store the current transaction in a directory
