# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: check-upgrade [options] [<package-spec>...]
description: check for available package upgrades
flags:
    --changelogs: print the package changelogs
    --json: request JSON output format
    --minimal: reports the lowest versions of packages that fix advisories
