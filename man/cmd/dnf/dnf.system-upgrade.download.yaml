# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: download
description: download packages needed for upgrade
flags:
    --allowerasing: allow removing of installed packages
    --no-downgrade: do not install older packages
    --releasever=: 'required: the version to upgrade to'
