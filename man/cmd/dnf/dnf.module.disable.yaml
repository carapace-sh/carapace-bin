# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: disable <module-spec>...
description: disable modules
flags:
    --skip-broken: resolve dependency problems
    --skip-unavailable: allow skipping modules
