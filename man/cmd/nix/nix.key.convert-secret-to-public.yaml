# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: convert-secret-to-public
description: generate a public key for verifying store paths from a secret key read from standard input
