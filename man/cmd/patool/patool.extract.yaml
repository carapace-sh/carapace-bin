# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: extract
description: extract one or more archives
flags:
    --outdir=: output directory to extract to
    --password=: password for encrypted files
