# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: describe
description: Show details of OpenFaaS function from a store
flags:
    -v, --verbose: Verbose output for the field values
