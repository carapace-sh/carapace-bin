# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: acl
description: Interact with Consul's ACLs

documentation:
  command: >-
    Manage Consul ACL (Access Control List) policies, tokens, and roles for securing the cluster.
