# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: delete
description: Delete a centralized config entry
flags:
    --kind=: The kind of configuration to delete.
    --name=: The name of configuration to delete.
