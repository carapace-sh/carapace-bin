# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: cat [charm:]PATH
description: Output the content of the file at path.

documentation:
  command: >-
    Output the content of a file at the specified path from Charm Cloud storage.
