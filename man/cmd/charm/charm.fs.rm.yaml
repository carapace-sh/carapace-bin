# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: rm [charm:]PATH
description: Remove file or directory at path

documentation:
  command: >-
    Remove a file or directory from Charm Cloud storage.
