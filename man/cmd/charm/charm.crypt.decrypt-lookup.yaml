# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: decrypt-lookup
description: Decrypt arg deterministically

documentation:
  command: >-
    Decrypt a deterministically encrypted value back to its original form.
