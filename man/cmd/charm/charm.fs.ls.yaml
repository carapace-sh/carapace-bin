# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: ls [charm:]PATH
description: List file or directory at path

documentation:
  command: >-
    List files and directories at the specified path in Charm Cloud storage.
