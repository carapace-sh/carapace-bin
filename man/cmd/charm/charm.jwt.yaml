# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: jwt
description: Print a JWT

documentation:
  command: >-
    Generate and print a JSON Web Token (JWT) for your Charm account.
