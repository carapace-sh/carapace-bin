# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: fs
description: Use the Charm file system.

documentation:
  command: >-
    Use the Charm Cloud file system. Read, write, and manage files stored in the cloud.
