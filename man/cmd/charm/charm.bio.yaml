# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: bio
hidden: true

documentation:
  command: >-
    Print the authenticated user's bio information from the Charm server as JSON.
