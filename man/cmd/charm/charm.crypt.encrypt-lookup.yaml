# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: encrypt-lookup
description: Encrypt arg deterministically

documentation:
  command: >-
    Deterministically encrypt an argument for lookup purposes. Same input always produces the same output.
