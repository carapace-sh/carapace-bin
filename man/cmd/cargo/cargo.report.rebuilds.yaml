# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: rebuilds
description: Reports rebuild reasons from previous sessions (unstable)
flags:
    --id=: Session ID to report on
    --manifest-path=: Path to Cargo.toml
    -h, --help: Print help
