# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: converge [flags]
description: Use the provisioner to configure instances
flags:
    --help: Show help message and exit
    -s, --scenario-name=: Name of the scenario to target
