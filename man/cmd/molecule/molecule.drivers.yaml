# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: drivers [flags]
description: List drivers
flags:
    --help: Show help message and exit
    -f, --format=: Change output format
