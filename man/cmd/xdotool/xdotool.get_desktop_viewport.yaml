# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: get_desktop_viewport
description: Report the current viewport's position
flags:
    --shell: output friendly to shell eval
