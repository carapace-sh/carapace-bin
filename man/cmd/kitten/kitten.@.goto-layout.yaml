# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: goto-layout
description: Set the window layout
flags:
    -h, --help: Show help for this command
    -m, --match=: The tab to match
