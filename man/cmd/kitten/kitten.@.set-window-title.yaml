# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: set-window-title
description: Set the window title
flags:
    --temporary: By default, the title will be permanently changed and programs running in the window will not be able to change it again
    -h, --help: Show help for this command
    -m, --match=: The window to match
