# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: kitten
description: Run a kitten
flags:
    -h, --help: Show help for this command
    -m, --match=: The window to match
