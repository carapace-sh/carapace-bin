# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: set-user-vars
description: Set user variables on a window
flags:
    -h, --help: Show help for this command
    -m, --match=: The window to match
