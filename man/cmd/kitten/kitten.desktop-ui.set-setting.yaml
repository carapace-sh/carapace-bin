# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: set-setting
description: Change an arbitrary setting
flags:
    --data-type=: The DBUS data type signature of the value
    -h, --help: Show help for this command
    -n, --namespace=: The namespace in which to change the setting
