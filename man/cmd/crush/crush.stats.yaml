# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: stats
description: Show usage statistics
documentation:
    command: |
        Display Crush usage statistics, including prompts run, tokens
        consumed, and sessions created.
