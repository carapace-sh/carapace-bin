# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: ls
description: List all variables for the specified Environment

documentation:
  command: >-
    List all environment variables for the project with their scopes.
