# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: deploy
description: Performs a deployment

documentation:
  command: >-
    Deploy the current project to Vercel. Creates a preview deployment by default, or production with --prod.
