# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: add
description: Add a new project

documentation:
  command: >-
    Create a new Vercel project linked to a Git repository.
