# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: add
description: Add a new domain that you already own

documentation:
  command: >-
    Add an existing domain to your Vercel account. You must configure DNS separately.
