# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: init
description: Initialize an example project

documentation:
  command: >-
    Initialize a new project with a framework template.
