# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: add
description: Add a new DNS entry

documentation:
  command: >-
    Add a new DNS record (A, CNAME, TXT, MX, etc.) for a domain.
