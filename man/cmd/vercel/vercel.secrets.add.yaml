# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: add
description: Add a new secret

documentation:
  command: >-
    Create a new named secret for use in environment variables.
