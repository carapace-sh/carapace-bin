# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: link
description: Link local directory to a Vercel Project

documentation:
  command: >-
    Link the local directory to a Vercel project for deployments.
