# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: whoami
description: Shows the username of the currently logged in user

documentation:
  command: >-
    Display the username and team of the currently logged-in account.
