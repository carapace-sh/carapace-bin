# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: transfer-in
description: Transfer in a domain to Vercel

documentation:
  command: >-
    Transfer a domain from another registrar to Vercel. The domain must be unlocked at the current registrar and the transfer authorisation code must be provided.
