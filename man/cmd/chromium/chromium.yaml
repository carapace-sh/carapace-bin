# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
documentation:
  command: >-
    Chromium web browser — the open-source project behind Google
    Chrome. Launches the browser window.
description: chromium browser
