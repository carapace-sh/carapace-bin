# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: init
aliases:
    - i
description: Creates a sample nfpm.yaml config file
flags:
    -f, --config=: path to the to-be-created config file
