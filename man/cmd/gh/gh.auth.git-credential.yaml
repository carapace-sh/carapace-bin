# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: git-credential
description: Implements git credential helper protocol
hidden: true
documentation:
    command: >-
        Git credential helper implementation for GitHub CLI. Called by git internally to provide credentials for GitHub hosts.
