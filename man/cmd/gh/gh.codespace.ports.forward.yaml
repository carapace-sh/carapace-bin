# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: forward <remote-port>:<local-port>...
description: Forward ports
documentation:
    command: >-
        Forward a port from a codespace to a local port.
