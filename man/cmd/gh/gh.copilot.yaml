# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: copilot [flags] [args]
description: Run the GitHub Copilot CLI (preview)
flags:
    --remove: Remove the downloaded Copilot CLI
documentation:
    command: >-
        Run the GitHub Copilot CLI for AI-assisted command suggestions. This feature is in preview.
