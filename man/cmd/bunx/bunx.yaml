# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
documentation:
  command: >-
    Run a command from a local or remote npm package without
    globally installing it.
description: bun package manager
