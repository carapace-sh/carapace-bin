# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: doc
description: Generate Markdown documentation for the Hugo CLI.
persistentflags:
    --dir=: the directory to write the doc.
