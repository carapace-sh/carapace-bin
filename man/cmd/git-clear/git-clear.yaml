# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: git-clear
description: Rigorously clean up a repository
flags:
    -f, --force: Force the clean, with no warning to the user
    -h, --help: show help
