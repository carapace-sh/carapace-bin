# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
aliases:
    - ls
description: List the platforms in the workspace file
flags:
    --json: Emit machine-readable JSON instead of the human view
