# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: info
description: List available generations from profile path
flags:
    --fields=: Comma-delimited list of field(s) to display
    -P, --profile=: Path to Nix profiles directory
