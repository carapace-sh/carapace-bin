# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: repl
description: Load a nix-darwin configuration in a Nix REPL
flags:
    -H, --hostname=: Select this hostname from darwinConfigurations
