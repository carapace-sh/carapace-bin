# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: export
flags:
    -F, --force: bypass schema testing
    -f, --format=: output format
    -h, --help: display help for command
    -t, --theme=: theme name
