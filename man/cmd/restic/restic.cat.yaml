# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: cat
description: Print internal objects to stdout

documentation:
  command: >-
    Print raw repository objects (packs, indexes, snapshots, keys) to stdout for debugging.
