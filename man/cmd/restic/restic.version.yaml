# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: version
description: Print version information

documentation:
  command: >-
    Print the restic version, Go version, and build platform. Use the global `--json` flag for machine-readable output.
