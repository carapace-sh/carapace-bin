# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list
description: List objects in the repository

documentation:
  command: >-
    List objects in the repository by type: blobs, packs, indexes, snapshots, or keys.
