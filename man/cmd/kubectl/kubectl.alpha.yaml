# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: alpha
description: Commands for features in alpha

documentation:
  command: >-
    Commands for features in alpha. These commands are not enabled in Kubernetes clusters by default.
