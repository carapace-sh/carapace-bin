# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: get
description: Show cache contents
flags:
    -h, --help: Print help
completion:
    flag:
        C:
            - $files
        config:
            - $files
        format:
            - text
            - json
