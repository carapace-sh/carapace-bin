# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: clear
description: Drop all caches
flags:
    -h, --help: Print help
completion:
    flag:
        C:
            - $files
        config:
            - $files
        format:
            - text
            - json
