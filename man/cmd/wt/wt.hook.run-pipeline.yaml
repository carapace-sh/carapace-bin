# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: run-pipeline
description: 'Internal: run a serialized pipeline from stdin'
hidden: true
flags:
    -h, --help: Print help
completion:
    flag:
        C:
            - $files
        config:
            - $files
