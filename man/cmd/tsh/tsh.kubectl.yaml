# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: kubectl
description: Runs a kubectl command on a Kubernetes cluster.

documentation:
  command: >-
    Run kubectl commands against a Kubernetes cluster through Teleport, handling authentication automatically.
