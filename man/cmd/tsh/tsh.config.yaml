# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: config
description: Print OpenSSH configuration details.

documentation:
  command: >-
    Print OpenSSH configuration for connecting to Teleport nodes via standard SSH clients.
