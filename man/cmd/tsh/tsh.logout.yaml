# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: logout
description: Delete a cluster certificate.

documentation:
  command: >-
    Delete the stored session certificate for a cluster, effectively ending the session.
