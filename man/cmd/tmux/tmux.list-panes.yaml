# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: list-panes
flags:
    -F=: format
    -a: TODO description
    -f=: filter
    -s: TODO description
    -t=: target-window
