nowarn: |
  Git will do nothing when a carriage return followed by a line feed (CRLF) is found in the commit message.
warn: |
  Git will issue a warning for each message if such a CRLF is found in the commit message.
strip: |
  Git will convert those CRLF to LF in the commit message.
